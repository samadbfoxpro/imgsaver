<Window x:Class="imgsaver.CharacterEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Characters" 
        Width="550" Height="500"
        MinWidth="450" MinHeight="400"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI" 
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" 
                      ResizeBorderThickness="5" 
                      GlassFrameThickness="0"
                      CornerRadius="8"/>
    </WindowChrome.WindowChrome>

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Background="{StaticResource BackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/> <!-- Title Bar -->
                <RowDefinition Height="*"/>  <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource TitleBarBrush}" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
                        <TextBlock Text="⚙️" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Manage Characters" Foreground="{StaticResource ForegroundBrush}" 
                                   FontSize="12" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button x:Name="BtnClose" Content="✕" Click="BtnClose_Click"
                                Style="{StaticResource TitleBarCloseButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- List Header -->
                    <RowDefinition Height="*"/>    <!-- Character List -->
                    <RowDefinition Height="Auto"/> <!-- Edit Form -->
                </Grid.RowDefinitions>

                <!-- Character List Header -->
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock Text="CHARACTER LIBRARY" Foreground="{StaticResource AccentBrush}" 
                               FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center"/>
                    <Button x:Name="BtnAddNew" Content="+ Add New" Click="BtnAddNew_Click"
                            HorizontalAlignment="Right" Padding="12,4" FontSize="11"/>
                </Grid>

                <!-- Character List -->
                <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="4" 
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <ListBox x:Name="CharacterList" SelectionChanged="CharacterList_SelectionChanged"
                             Background="Transparent" BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding ShortName}" FontWeight="SemiBold" FontSize="13"
                                                   Foreground="{StaticResource ForegroundBrush}"/>
                                        <TextBlock Text="{Binding FullPersona}" FontSize="11" TextTrimming="CharacterEllipsis"
                                                   Foreground="{StaticResource ForegroundMutedBrush}" MaxWidth="350"/>
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="🗑️" Tag="{Binding Id}" Click="BtnDelete_Click"
                                            Background="Transparent" BorderThickness="0" FontSize="14"
                                            Foreground="{StaticResource ForegroundMutedBrush}" Cursor="Hand" Padding="8,4"
                                            ToolTip="Delete Character"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Padding" Value="8,4"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                                    Padding="{TemplateBinding Padding}" CornerRadius="4">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="{StaticResource SelectedBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </Border>

                <!-- Edit Form -->
                <Border Grid.Row="2" Margin="0,12,0,0" Background="{StaticResource SurfaceBrush}" 
                        CornerRadius="4" Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" x:Name="TxtFormTitle" Text="ADD NEW CHARACTER" 
                                   Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,8"/>
                        
                        <Grid Grid.Row="1" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Name:" Foreground="{StaticResource ForegroundMutedBrush}" 
                                       VerticalAlignment="Center" FontSize="12"/>
                            <TextBox x:Name="TxtCharacterName" Grid.Column="1" FontSize="12" Padding="8,4" VerticalContentAlignment="Center"/>
                        </Grid>

                        <Grid Grid.Row="2" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Persona:" Foreground="{StaticResource ForegroundMutedBrush}" 
                                       VerticalAlignment="Top" FontSize="12" Margin="0,6,0,0"/>
                            <TextBox x:Name="TxtCharacterPersona" Grid.Column="1" Height="60" FontSize="12" 
                                     AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Padding="8,4"/>
                        </Grid>

                        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="BtnCancel" Content="Cancel" Click="BtnCancel_Click"
                                    Style="{StaticResource SecondaryButtonStyle}" Padding="16,6" Margin="0,0,8,0"/>
                            <Button x:Name="BtnSave" Content="Save" Click="BtnSave_Click" Padding="16,6"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
