<Window x:Class="imgsaver.SnippetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text Snippet Manager" Height="550" Width="850"
        MinHeight="450" MinWidth="700"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" 
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        Icon="{x:Null}">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" 
                      ResizeBorderThickness="5" 
                      GlassFrameThickness="0"
                      CornerRadius="8"/>
    </WindowChrome.WindowChrome>

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Background="{StaticResource BackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/> <!-- Title Bar -->
                <RowDefinition Height="*"/>  <!-- Content -->
            </Grid.RowDefinitions>

            <!-- VS Code Style Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource TitleBarBrush}" CornerRadius="8,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
                        <TextBlock Text="⚙" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Snippet Manager" Foreground="{StaticResource ForegroundBrush}" 
                                   FontSize="12" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button x:Name="BtnMinimize" Content="─" Click="BtnMinimize_Click"
                                Style="{StaticResource TitleBarButtonStyle}"/>
                        <Button x:Name="BtnClose" Content="✕" Click="BtnClose_Click"
                                Style="{StaticResource TitleBarCloseButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel: Snippet List -->
                <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" CornerRadius="6" Padding="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Snippets List" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{StaticResource ForegroundMutedBrush}" Margin="4,0,0,12"/>

                        <ListBox x:Name="LstSnippets" Grid.Row="1" Background="Transparent" BorderThickness="0" 
                                 SelectionChanged="LstSnippets_SelectionChanged" DisplayMemberPath="Key"/>

                        <Button Grid.Row="2" Content="➕ Add New" Click="BtnNew_Click" Margin="0,12,0,0" 
                                Height="38" FontWeight="SemiBold"/>
                    </Grid>
                </Border>

                <!-- Right Panel: Editor -->
                <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" CornerRadius="6" Padding="16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Edit / Add" FontSize="16" FontWeight="SemiBold" 
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,16"/>

                        <!-- Short Key Input -->
                        <StackPanel Grid.Row="1" Margin="0,0,0,12">
                            <TextBlock Text="Keyword (Short Key)" Foreground="{StaticResource AccentBrush}" 
                                       FontSize="11" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtKey" FontSize="14" Height="38" VerticalContentAlignment="Center" 
                                     ToolTip="e.g., /dog"/>
                            <TextBlock Text="Example: /test (must start with / to avoid conflict)" 
                                       FontSize="10" Foreground="{StaticResource ForegroundMutedBrush}" Margin="0,4,0,0"/>
                        </StackPanel>

                        <!-- Full Text Input -->
                        <DockPanel Grid.Row="2">
                            <TextBlock DockPanel.Dock="Top" Text="Replacement Text (Full Text)" 
                                       Foreground="{StaticResource ForegroundMutedBrush}" 
                                       FontSize="11" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtValue" FontSize="13" TextWrapping="Wrap" AcceptsReturn="True" 
                                     VerticalScrollBarVisibility="Auto" Padding="10" VerticalContentAlignment="Top"/>
                        </DockPanel>

                        <!-- Action Buttons -->
                        <Grid Grid.Row="3" Margin="0,16,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0" Content="💾 Save Changes" Click="BtnSave_Click" 
                                    Height="42" FontSize="14" FontWeight="SemiBold"/>

                            <Button Grid.Column="1" Content="🗑 Delete" Click="BtnDelete_Click" 
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Width="90" Height="42" Margin="12,0,0,0"
                                    Background="#C42B1C" Foreground="White" BorderThickness="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="3" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#D63B2E"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
            <!-- Particle Canvas (Overlay) -->
            <Canvas x:Name="ParticleCanvas" Grid.ColumnSpan="3" Grid.RowSpan="2" IsHitTestVisible="False" ClipToBounds="False"/>
        </Grid>
    </Border>
</Window>