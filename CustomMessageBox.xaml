<Window x:Class="imgsaver.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm" 
        SizeToContent="WidthAndHeight"
        MinWidth="350" MaxWidth="550"
        MinHeight="150"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" 
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" 
                      ResizeBorderThickness="0" 
                      GlassFrameThickness="0"
                      CornerRadius="12"/>
    </WindowChrome.WindowChrome>

    <Border CornerRadius="12" BorderThickness="1" BorderBrush="#444">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#252525" Offset="0"/>
                <GradientStop Color="#151515" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="12" Opacity="0.6" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/> <!-- Title Bar -->
                <RowDefinition Height="*"/>  <!-- Message -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="Transparent" CornerRadius="12,12,0,0"
                    Padding="16,0">
                <TextBlock x:Name="TxtTitle" Text="Confirm" Foreground="{StaticResource ForegroundMutedBrush}" 
                           FontSize="11" VerticalAlignment="Center" FontWeight="SemiBold"/>
            </Border>

            <!-- Message Content -->
            <Grid Grid.Row="1" Margin="24,10,24,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Icon -->
                <TextBlock x:Name="TxtIcon" Text="❓" FontSize="32" VerticalAlignment="Top" Margin="0,0,24,0" 
                           Grid.Column="0" Foreground="{StaticResource AccentBrush}"/>

                <!-- Message Text -->
                <TextBlock x:Name="TxtMessage" Text="Are you sure?" 
                           Grid.Column="1"
                           Foreground="{StaticResource ForegroundBrush}" 
                           FontSize="13" TextWrapping="Wrap" LineHeight="22"/>
            </Grid>

            <!-- Buttons Layer -->
            <Border Grid.Row="2" Background="#1A1A1A" Padding="16" CornerRadius="0,0,12,12" BorderBrush="#333" BorderThickness="0,1,0,0">
                <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Right">
                    <StackPanel.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="Height" Value="30"/>
                            <Setter Property="MinWidth" Value="70"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Margin" Value="6,0,0,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="14">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#555"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                        
                        <!-- Secondary Button Style -->
                         <Style x:Key="SecondaryMsgButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                            <Setter Property="Height" Value="30"/>
                            <Setter Property="MinWidth" Value="70"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Margin" Value="6,0,0,0"/>
                             <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="Transparent" 
                                                BorderBrush="{StaticResource BorderBrush}" 
                                                BorderThickness="1" 
                                                CornerRadius="14">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#33FFFFFF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </StackPanel.Resources>

                    <Button x:Name="BtnYes" Content="Yes" Click="BtnYes_Click"/>
                    
                    <Button x:Name="BtnNo" Content="No" Click="BtnNo_Click" Style="{StaticResource SecondaryMsgButtonStyle}"/>
                    
                    <Button x:Name="BtnOK" Content="OK" Click="BtnOK_Click" Visibility="Collapsed"/>
                    
                    <Button x:Name="BtnCancel" Content="Cancel" Click="BtnCancel_Click" Style="{StaticResource SecondaryMsgButtonStyle}" Visibility="Collapsed"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
